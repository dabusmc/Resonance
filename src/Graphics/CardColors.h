#pragma once

#include "Colors.h"

namespace Resonance
{
	class CardColors
	{
	public:
		static Color GetColor1(ColorMode mode);
		static Color GetColor2(ColorMode mode);
		static Color GetColor3(ColorMode mode);
		static Color GetColor4(ColorMode mode);
	};
}
